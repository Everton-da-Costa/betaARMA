% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BARMAX_ridge_bootstrap_seasonal_parallel.R
\name{BARMAX_ridge_bootstrap_seasonal_parallel}
\alias{BARMAX_ridge_bootstrap_seasonal_parallel}
\title{Parallelized Seasonal Block Bootstrap for BARMA(X) Models}
\usage{
BARMAX_ridge_bootstrap_seasonal_parallel(
  n_cores = 1,
  y = NULL,
  seed = 3,
  ar_vec = NA,
  ma_vec = NA,
  fit_ridge = NULL,
  penalty = NULL,
  regressors_list = NULL,
  block_length = NULL,
  n_boot_rep = 200
)
}
\arguments{
\item{n_cores}{Number of CPU cores for parallel processing. Defaults to 1.}

\item{y}{The time series data, typically a 'ts' object.}

\item{seed}{Integer for the random number generator seed for reproducibility.}

\item{ar_vec}{Numeric vector for autoregressive (AR) lags.}

\item{ma_vec}{Numeric vector for moving average (MA) lags.}

\item{fit_ridge}{An object from the `barma` function for the original data.}

\item{penalty}{The penalty term (lambda) for the ridge regression.}

\item{regressors_list}{A list containing the time series (`y_sample_ts`) and
all required regressor vectors (`hs`, `hc`, `hs_hat`, `hc_hat`).}

\item{block_length}{Integer specifying the block size for the bootstrap.}

\item{n_boot_rep}{The number of bootstrap replications. Defaults to 200.}
}
\value{
A list containing the bootstrap results, including a matrix of coefficient
estimates (`mat_boot_estimates`) and their means
(`mean_bootstrap_estimates`), along with convergence statistics.

A list containing the bootstrap results, including a matrix of coefficient
estimates (`mat_boot_estimates`) and their means
(`mean_bootstrap_estimates`), along with convergence statistics.
}
\description{
Implements a robust, parallelized seasonal block bootstrap for BARMA(X) models.
This function is designed to provide stable parameter estimates by resampling
the data and refitting a ridge-penalized model, as described in
Cribari-Neto, Costa, and Fonseca (2025). It includes a retry mechanism to
handle model non-convergence.
}
\references{
Cribari-Neto, F., Costa, E., & Fonseca, R. V. (2025). Numerical stability
enhancements in beta autoregressive moving average model estimation.
*Manuscript submitted for publication*.

Dudek, A. E., Leskow, J., Paparoditis, E., & Politis, D. N. (2014).
A GENERALIZED BLOCK BOOTSTRAP FOR SEASONAL TIME SERIES.
*Journal of Time Series Analysis*, 35(2), 89–114.
}
